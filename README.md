# TG Feed Bot (Lite Rewrite)

Легкий и быстрый Telegram бот для мониторинга RSS/Atom лент и публикации обновлений в каналы. 
Полностью переписанная версия на базе `asyncio` и `aiohttp`, оптимизированная для работы на слабых VPS (потребление памяти <50МБ).

> **Credits:** Этот проект является форком и полной переработкой [tg-feed-bot](https://github.com/fossunited/tg-feed-bot) от FOSS United. Оригинальная идея сохранена, но кодовая база полностью обновлена для поддержки асинхронности, SQLite и множественных подписок.

## Возможности

*   🚀 **Асинхронность**: Эффективно обрабатывает десятки фидов одновременно.
*   📝 **Множественные каналы**: Поддержка неограниченного количества RSS-лент и целевых каналов в одном боте.
*   🎨 **Шаблонизация**: Настраиваемый формат сообщений для каждого фида (заголовок, ссылка, дата, автор).
*   🧹 **Очистка ссылок**: Автоматическое удаление UTM-меток и трекеров из URL.
*   💾 **SQLite**: Надежное хранение истории публикаций в одном файле.
*   🐳 **Docker**: Готов к запуску через Docker Compose.

## Установка и запуск

### Через Docker (Рекомендуется)

1.  Клонируйте репозиторий.
2.  Создайте конфигурацию:
    ```bash
    cp sample_config.json config.json
    ```
3.  Отредактируйте `config.json`, указав токен бота и ваши фиды.
4.  Запустите:
    ```bash
    docker-compose up -d --build
    ```

### Локальный запуск (Python 3.11+)

1.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
2.  Создайте `config.json` (см. `sample_config.json`).
3.  Запустите бота:
    ```bash
    python feedbot.py
    ```

## Конфигурация

Пример `config.json`:

```json
{
  "telegram_token": "ВАШ_ГЛОБАЛЬНЫЙ_ТОКЕН",
  "database_path": "data/feedbot.db",
  "feeds": [
    {
        "name": "Habr",
        "url": "https://habr.com/rss/best/",
        "target_chat_id": "@channel_name",
        "check_interval": 600
    },
    {
        "name": "Private Feed",
        "url": "https://example.com/feed.xml",
        "telegram_token": "ТОКЕН_ДРУГОГО_БОТА",
        "target_chat_id": -1001234567890
    }
  ]
}
```

### Форматы `target_chat_id`
*   `@channelname` — Публичный канал (бот должен быть админом).
*   `-100xxxxxxxxx` — Приватный канал или супергруппа (ID всегда начинается с `-100`).
*   `12345678` — Личные сообщения с пользователем (пользователь должен сначала написать боту `/start`).

### Дополнительные опции
*   `telegram_token` (внутри объекта feed): Позволяет использовать **отдельного бота** для конкретного фида. Если не указано, используется глобальный токен.

### Доступные переменные шаблона
*   `{title}`: Заголовок записи
*   `{link}`: Ссылка (очищенная от мусора)
*   `{published}`: Дата публикации (YYYY-MM-DD HH:MM)
*   `{author}`: Автор записи

## Лицензия

Код распространяется свободно. Оригинальная лицензия проекта-прародителя: MIT.
